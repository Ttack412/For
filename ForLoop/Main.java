class Main {
  public static void main(String[] args) {
    String[] instruments = {"Piano", "Guitar", "Drums", "Keyboard", "Harmonica", "Violin"};

    // 1. Create a for loop that prints out the instruments in the array above. 
    for(int i = 0; i < instruments.length; i++)
      {
        System.out.println(instruments[i]);
      }
  
  }
}